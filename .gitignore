# Secrets and local financial data — never commit these.
server/.env
server/data/
server/node_modules/
node_modules/

# OS noise
Thumbs.db
.DS_Store

# Server output — carries institution names and sync detail.
server/console.log
server/console.log.err
server/console.log.old

# Tunnel output and the current ephemeral URL — machine-local, changes constantly.
server/tunnel.log
server/tunnel.log.out
current-url.txt
